import { Container, Typography } from "@material-ui/core";

const Header = () => {
  return (
    <header>
      <Container>
        <Typography>Header</Typography>
      </Container>
    </header>
  );
};

export default Header;
